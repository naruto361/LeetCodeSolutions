bool stoneGame(vector<int>& piles) {
        // if(piles.size()==2) return true;
        // int sum1=0,sum2=0;
        // for(int i=0;i<piles.size();i+=2) sum1+=piles[i];
        // for(int i=1;i<piles.size();i+=2) sum2+=piles[i]; 
        // return true;
        return true;
    }
